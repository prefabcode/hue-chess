{
  "name": "hue-chess",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.23.0"
  }
}
